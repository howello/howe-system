<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.howe.blog.mapper.BlogStatsMapper">

    <!-- 三总计数：一个 select 触达三张表，任一张缺失即抛 BadSqlGrammarException 触发整体降级 -->
    <select id="selectTotalCounts" resultType="java.util.Map">
        select
          (select count(*) from blog_article) as article_total,
          (select count(*) from blog_draft where status = '0') as draft_total,
          (select count(*) from blog_talk) as talk_total
    </select>

    <select id="selectArticleCountByDay" resultType="java.util.Map">
        select date_format(date(publish_date), '%Y-%m-%d') as d_date, count(*) as cnt
        from blog_article
        where publish_date is not null
          and date(publish_date) between #{startDate} and #{endDate}
        group by date_format(date(publish_date), '%Y-%m-%d')
    </select>

    <select id="selectDraftCountByDay" resultType="java.util.Map">
        select date_format(date(create_time), '%Y-%m-%d') as d_date, count(*) as cnt
        from blog_draft
        where status = '0' and create_time is not null
          and date(create_time) between #{startDate} and #{endDate}
        group by date_format(date(create_time), '%Y-%m-%d')
    </select>

    <select id="selectTalkCountByDay" resultType="java.util.Map">
        select date_format(date(create_time), '%Y-%m-%d') as d_date, count(*) as cnt
        from blog_talk
        where create_time is not null
          and date(create_time) between #{startDate} and #{endDate}
        group by date_format(date(create_time), '%Y-%m-%d')
    </select>

    <select id="selectAllArticleCategories" resultType="java.lang.String">
        select categories from blog_article
        where categories is not null and categories != ''
    </select>

</mapper>
