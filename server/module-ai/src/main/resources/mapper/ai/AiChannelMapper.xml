<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.howe.ai.persistence.mapper.AiChannelMapper">
    <resultMap type="AiChannel" id="AiChannelResult">
        <result property="id"           column="id"            />
        <result property="providerCode" column="provider_code" />
        <result property="name"         column="name"          />
        <result property="apiKey"       column="api_key"       />
        <result property="baseUrl"      column="base_url"      />
        <result property="extra"        column="extra"         />
        <result property="priority"     column="priority"      />
        <result property="enabled"      column="enabled"       />
        <result property="healthStatus" column="health_status" />
        <result property="lastCheckAt"  column="last_check_at" />
        <result property="remark"       column="remark"        />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
    </resultMap>

    <sql id="selectAiChannelVo">
        select id, provider_code, name, api_key, base_url, extra, priority, enabled,
               health_status, last_check_at, remark, create_by, create_time, update_by, update_time
        from ai_channel
    </sql>

    <select id="selectAiChannelList" parameterType="AiChannel" resultMap="AiChannelResult">
        <include refid="selectAiChannelVo"/>
        <where>
            <if test="providerCode != null and providerCode != ''">
                AND provider_code = #{providerCode}
            </if>
            <if test="name != null and name != ''">
                AND name like concat('%', #{name}, '%')
            </if>
            <if test="enabled != null">
                AND enabled = #{enabled}
            </if>
            <if test="healthStatus != null and healthStatus != ''">
                AND health_status = #{healthStatus}
            </if>
        </where>
        order by priority asc, id asc
    </select>

    <select id="selectAiChannelById" parameterType="Long" resultMap="AiChannelResult">
        <include refid="selectAiChannelVo"/>
        where id = #{id}
    </select>

    <select id="selectEnabledList" resultMap="AiChannelResult">
        <include refid="selectAiChannelVo"/>
        where enabled = 1
        order by priority asc, id asc
    </select>

    <select id="countByProviderCode" parameterType="String" resultType="int">
        select count(1) from ai_channel where provider_code = #{providerCode}
    </select>

    <insert id="insertAiChannel" parameterType="AiChannel" useGeneratedKeys="true" keyProperty="id">
        insert into ai_channel (
            provider_code, name, api_key, base_url, extra, priority, enabled,
            health_status, remark, create_by, create_time
        ) values (
            #{providerCode}, #{name}, #{apiKey}, #{baseUrl}, #{extra}, #{priority}, #{enabled},
            #{healthStatus}, #{remark}, #{createBy}, sysdate()
        )
    </insert>

    <update id="updateAiChannel" parameterType="AiChannel">
        update ai_channel
        <set>
            <if test="providerCode != null and providerCode != ''">provider_code = #{providerCode},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="baseUrl != null">base_url = #{baseUrl},</if>
            <if test="extra != null">extra = #{extra},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="healthStatus != null and healthStatus != ''">health_status = #{healthStatus},</if>
            <if test="lastCheckAt != null">last_check_at = #{lastCheckAt},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <update id="updateApiKey">
        update ai_channel
        set api_key = #{apiKey}, update_by = #{updateBy}, update_time = sysdate()
        where id = #{id}
    </update>

    <update id="updateHealth">
        update ai_channel
        set health_status = #{healthStatus}, last_check_at = #{lastCheckAt}, update_time = sysdate()
        where id = #{id}
    </update>

    <delete id="deleteAiChannelByIds" parameterType="Long">
        delete from ai_channel where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
