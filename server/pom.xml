<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

    <groupId>com.howe</groupId>
    <artifactId>server</artifactId>
    <version>3.9.2</version>

    <name>server</name>
    <description>后台管理系统</description>

    <properties>
        <howe.version>3.9.2</howe.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <spring-boot.version>3.5.14</spring-boot.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <druid.version>1.2.28</druid.version>
        <yauaa.version>8.1.1</yauaa.version>
        <pagehelper.boot.version>2.1.1</pagehelper.boot.version>
        <fastjson.version>2.0.62</fastjson.version>
        <oshi.version>7.3.0</oshi.version>
        <commons.io.version>2.22.0</commons.io.version>
        <poi.version>4.1.2</poi.version>
        <velocity.version>2.3</velocity.version>
        <jwt.version>0.9.1</jwt.version>
        <jaxb-api.version>2.3.1</jaxb-api.version>
        <springdoc.version>2.8.17</springdoc.version>
        <!-- 与 springdoc 2.8.17 内部管理的 swagger-api 版本保持一致，错版会导致注解与 core 不匹配 -->
        <swagger-annotations.version>2.2.47</swagger-annotations.version>
        <aws-sdk.version>2.46.21</aws-sdk.version>
        <hutool.version>5.8.36</hutool.version>
        <rome.version>2.1.0</rome.version>
        <playwright.version>1.62.0</playwright.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- SpringBoot的依赖配置 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- AWS SDK v2 依赖配置（S3 兼容协议，用于对接 Cloudflare R2） -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws-sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Hutool 工具集：HTTP 请求、文件与 IO 工具 -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-bom</artifactId>
                <version>${hutool.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ROME：RSS/Atom 解析。
                 这是「网络请求、文件与 IO 工具统一用 hutool」约定的显式例外：
                 抓取目标是不受控的第三方博客，RSS 2.0 与 Atom 的字段差异、
                 RFC822/ISO8601 的多种日期变体都需大量实战校正，手写解析会持续踩坑。
                 HTTP 取流仍用 hutool HttpRequest，ROME 只负责解析已取回的字节流。 -->
            <dependency>
                <groupId>com.rometools</groupId>
                <artifactId>rome</artifactId>
                <version>${rome.version}</version>
            </dependency>

            <!-- 阿里数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- 解析客户端操作系统、浏览器等 -->
            <dependency>
                <groupId>nl.basjes.parse.useragent</groupId>
                <artifactId>yauaa</artifactId>
                <version>${yauaa.version}</version>
            </dependency>

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.boot.version}</version>
            </dependency>

            <!-- MyBatis-Plus（Spring Boot 3 对应组件） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>${jaxb-api.version}</version>
            </dependency>

            <!-- 获取系统信息 -->
            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>

            <!-- spring-doc -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- swagger 注解：springdoc 只装在 module-admin，其余模块要写 @Tag/@Operation/@Schema
                 就得单独引这个纯注解包（不含任何运行时引擎） -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- velocity代码生成使用模板 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- 阿里JSON解析器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- Token生成与解析-->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jwt.version}</version>
            </dependency>

            <!-- 定时任务-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-quartz</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 代码生成-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-generator</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 核心模块-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-framework</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 系统模块-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-system</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 博客模块-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-blog</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 通用工具-->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-common</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- 浏览器自动化 -->
            <dependency>
                <groupId>com.howe</groupId>
                <artifactId>module-automation</artifactId>
                <version>${howe.version}</version>
            </dependency>

            <!-- Playwright Java -->
            <dependency>
                <groupId>com.microsoft.playwright</groupId>
                <artifactId>playwright</artifactId>
                <version>${playwright.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- 所有子模块共享的依赖（写在 dependencies 而非 dependencyManagement，子模块无需重复声明） -->
    <dependencies>

        <!-- Lombok：新写的实体/DTO 用 @Data/@Getter/@Setter，日志用 @Slf4j。
             provided 作用域只在编译期生效，不会打进最终 jar。
             存量实体保持手写 getter/setter 原样，不做批量改造 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <modules>
        <module>module-admin</module>
        <module>module-framework</module>
        <module>module-system</module>
        <module>module-quartz</module>
        <module>module-generator</module>
        <module>module-blog</module>
        <module>module-common</module>
        <module>module-automation</module>
    </modules>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <parameters>true</parameters>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
            </plugin>
        </plugins>
    </build>
</project>
